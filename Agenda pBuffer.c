#include <stdio.h>
#include <stdlib.h>

#define TAM_PESSOA (sizeof(char) * 10 + sizeof(int) + sizeof(char) * 10)

    int main (){
        void *pBuffer = malloc (sizeof(int) * 2); // menu + qtd de pessoas


    }